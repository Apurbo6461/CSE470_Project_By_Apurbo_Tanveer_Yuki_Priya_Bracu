# Copy this file to .env and replace the value below with your MongoDB connection string
# For local MongoDB (default port) example:
# MONGODB_URI=mongodb://localhost:27017/pc_set_db

# Or use MongoDB Atlas connection string (replace <username>, <password>, <cluster-url>, <dbname>)
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/<dbname>?retryWrites=true&w=majority

MONGODB_URI=
